# Hosted auth broker (extension talks to this, not Neon directly)
VERTEX_HOSTED_AUTH_URL=https://auth-for-vertex-swarm.onrender.com

# Local dev against a local auth service:
# VERTEX_HOSTED_AUTH_URL=http://localhost:8080
